# Postgres connection string (Supabase, Vercel Postgres, Neon, or local).
# Supabase: use the "Connection pooling" URI for the app, the direct URI for migrations.
DATABASE_URL="postgresql://user:password@host:5432/prompt_library"

# API key for the AI-assisted "Optimize prompt" feature (MIN-17).
ANTHROPIC_API_KEY=""
